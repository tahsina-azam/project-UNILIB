import React from "react";
import RecentBookList from "./RecentBookList";
/**
 * Contains all the elements for showing recent book list to users
 * @returns {component} RecentBookList
 */
function RecentBooks() {
  return (
    <div>
      <RecentBookList />
    </div>
  );
}

export default RecentBooks;
